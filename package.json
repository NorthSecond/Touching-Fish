{
  "name": "touchfisheveryday",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@douyinfe/semi-ui": "^2.1.4",
    "@testing-library/jest-dom": "^5.16.1",
    "@testing-library/react": "^11.2.7",
    "@testing-library/user-event": "^12.8.3",
    "echarts": "^5.2.2",
    "jsonp": "^0.2.1",
    "normalize.css": "^8.0.1",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-scripts": "4.0.3"
  },
  "scripts": {
    "start": "SET NODE_OPTIONS=--openssl-legacy-provide && react-scripts start",
    "build": "SET NODE_OPTIONS=--openssl-legacy-provide && react-scripts build",
    "test": "SET NODE_OPTIONS=--openssl-legacy-provide && react-scripts test",
    "eject": "SET NODE_OPTIONS=--openssl-legacy-provide && react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@douyinfe/semi-icons": "^2.1.4"
  }
}
